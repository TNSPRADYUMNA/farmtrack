apiVersion: v1
kind: Service
metadata:
  name: farmtrack-web
  namespace: farmtrack
spec:
  type: NodePort
  selector:
    app: field-service
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30080
